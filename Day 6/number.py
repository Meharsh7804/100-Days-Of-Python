# Create a new list called numbers with five integers. Print the length of the list. Slice the numbers list to extract the last three elements and print the result.

numbers = [1,2,3,4,5]

len = len(numbers)
print(len)

print(numbers[len-3:len])